#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../redis-commander/bin/redis-commander.js" "$@"
  ret=$?
else 
  node  "$basedir/../redis-commander/bin/redis-commander.js" "$@"
  ret=$?
fi
exit $ret
